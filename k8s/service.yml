apiVersion: v1
kind: Service

metadata:
  name: trend-service
  namespace: trend

spec:
  type: LoadBalancer

  selector:
    app: trend-app

  ports:
    - port: 3000
      targetPort: 80
      protocol: TCP
